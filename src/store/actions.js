// import firebase from 'firebase/compat/app';

export default {
	// authAction({ commit }) {
	// 	firebase.auth().onAuthStateChanged((user) => {
	// 		if (user) {
	// 			commit('setUser', user);
	// 		} else {
	// 			commit('setUser', null);
	// 		}
	// 	});
	// },
};
